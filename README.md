# Intelligent-Access-System

Variables , conditionals , logical expressions python foundation 001 and 002

# Rules

Rules:
VIP → always allowed
age ≥ 18 AND has_ticket → allowed
age < 18 AND has_ticket → restricted access
otherwise → denied

👉 Output must explain WHY (not just result)
